---
'cloudburn': minor
---

Reuse fresh AWS discovery evidence in per-user storage, with normal, refresh, and off cache modes, an explicit cache directory and authorization context, and evidence freshness in table and JSON output.
