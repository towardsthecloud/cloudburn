import type { Rule } from '../shared/metadata.js';

// Intent: reserve GCP provider namespace for future community and core rules.
// TODO(cloudburn): add first-party GCP optimization rules after AWS maturity.
export const gcpRules: Rule[] = [];
