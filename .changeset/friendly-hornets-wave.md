---
"@cloudburn/rules": minor
---

Refactor live rule authoring to use SDK-owned discovery dataset dependencies and a typed `LiveResourceBag` instead of rule-level `liveDiscovery` wiring and fixed context arrays.
