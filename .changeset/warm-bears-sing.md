---
"@cloudburn/rules": minor
---

Add `staticDependencies` and `StaticResourceBag` to the rule contract so static IaC rules can consume normalized SDK datasets instead of filtering raw parsed resources.
