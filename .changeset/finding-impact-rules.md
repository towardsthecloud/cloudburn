---
'@cloudburn/rules': minor
---

Add optional `FindingImpact`/`FinancialEvidence` contracts and an `impact` field on `FindingMatch`, the `createFinancialEvidence` validation helper, `createAwsConfigRecordingFrequencyImpact` for the AWS Config recording-frequency rule, and Hub recommendation impact built from nullable source financial fields with an optional lookback window.
