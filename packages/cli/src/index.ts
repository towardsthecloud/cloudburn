// Intent: expose CLI construction helpers for testing and embedding.
// TODO(cloudburn): decide whether this package should expose programmatic API publicly.
export { createProgram, runCli } from './cli.js';
