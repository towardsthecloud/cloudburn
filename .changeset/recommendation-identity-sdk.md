---
'@cloudburn/sdk': minor
---

Apply deterministic action-aware finding precedence across native and Cost Optimization Hub rules using normalized `resourceKey`/`opportunityId` identity, select the freshest Hub recommendation summary per scoped identity, and expose normalized recommendation metadata on findings and evaluated resources.
