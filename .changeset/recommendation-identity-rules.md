---
'@cloudburn/rules': minor
---

Add normalized recommendation provenance and identity contracts (`EvidenceProvenance`, `RecommendationIdentity`, `FindingRecommendation`), the `createRecommendationMatch`, `getRecommendationIdentity`, `deduplicateRecommendationMatches`, `canonicalizeAwsResourceId`, and `createAwsCostOptimizationHubFindingMatch` helpers, and attach normalized `actionType`/`recommendation` metadata to action-bearing native and Cost Optimization Hub findings.
