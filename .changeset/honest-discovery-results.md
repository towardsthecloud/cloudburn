---
'@cloudburn/sdk': patch
cloudburn: patch
---

Mark discovery datasets unavailable when every regional load is denied, so affected rules are skipped instead of reported as passing against an empty dataset.
